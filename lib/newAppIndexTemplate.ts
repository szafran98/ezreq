// import all files

const AppSettings = {
    name: `${process.cwd()}`,
    entities: [],
    controllers: [],
    routes: [],
}

// eslint-disable-next-line import/prefer-default-export
export { AppSettings }
